{
  "name": "narratemyworld-functions",
  "description": "Cloud Functions (2nd gen) backend for NarrateMyWorld. Proxies Gemini Vision, ElevenLabs streaming TTS, and Featherless model routing.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "lint": "echo 'lint not configured' && exit 0"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {}
}
